# Normalize all text files to LF in the repo and in the working tree.
# Deterministic across OSes regardless of each contributor's core.autocrlf.
* text=auto eol=lf

# Treat known binaries as binary (never touch their bytes).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pfx binary
*.sppkg binary
